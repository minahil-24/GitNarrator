# GitNarrator
This browser extension analyzes GitHub repositories across all branches and commits, reads source code and file changes, and automatically generates a PowerPoint summarizing the project. It converts complex codebases into clear, structured slides showing purpose, architecture, features, and development progress
